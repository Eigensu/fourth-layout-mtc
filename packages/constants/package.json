{
  "name": "@fantasy11/constants",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "pnpm tsc",
    "dev": "pnpm tsc --watch",
    "lint": "pnpm eslint src/**/*.ts",
    "type-check": "pnpm tsc --noEmit",
    "clean": "rm -rf dist .turbo node_modules"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.0.0",
    "eslint": "8.57.0"
  }
}
